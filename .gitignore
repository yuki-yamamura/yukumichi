node_modules
.turbo
dist
.env
generated
storybook-static
*.tsbuildinfo

# Claude Code
.claude/worktrees/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
*.tfplan
tfplan
*.auto.tfvars

# Playwright
.playwright-mcp/

# Go
# `go build` in apps/functions/<name>/ produces a binary named after the module (= directory name)
apps/functions/cognito-custom-message/cognito-custom-message
